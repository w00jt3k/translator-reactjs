export type Dictionary = {
  companyName: string
  components: {
    header: {
      title: string
      github: string
      discord: string
    }
    screen: {
      translator: {
        loading: string
      }
    }
    message: {
      tryAgain: string
    }
  }
}
